const fruit1="Apple"
const fruit2="Banana"
console.log(`two fruits are : ${fruit1} and ${fruit2}`);//manipulate any kind of string , with dynamic value using( ``,${} )


//it line wise auto applay /n (newline) character
const multilineMessage=`This is a message
that spans across
multiple lines.`;
console.log(multilineMessage);

//it  
const a=5;
const b=10;
const message=`The Addition of ${a} and ${b} is ${a+b}`;
console.log(message);

const title = "Welcome";
const body = "This is an example of using template literals for HTML.";
const htmlTemplate = 
`   <div>
        <h1>${title}</h1>
        <p>${body}</p>
    </div>`;
console.log(htmlTemplate);


  