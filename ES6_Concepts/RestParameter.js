function restParam(value1,...value2)
{
    console.log(value1,value2);
    
}

restParam(1,2,3,34,4,5,5,3,2);

// function restParam1(...value2,val2){} if rest parameter used as 1st param it gives compilation error
